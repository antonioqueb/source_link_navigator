<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="purchase_order_form_inherit_source_link" model="ir.ui.view">
        <field name="name">purchase.order.form.source.link</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='origin']" position="after">
                <field name="source_sale_order_id"
                       invisible="not source_sale_order_id"
                       readonly="1"/>
                <field name="source_purchase_order_id"
                       invisible="not source_purchase_order_id"
                       readonly="1"/>
            </xpath>
        </field>
    </record>

</odoo>
